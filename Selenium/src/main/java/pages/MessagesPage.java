package pages;
/*
import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;

public class MessagesPage {
    WebDriver driver;
    public MessagesPage(WebDriver driver){this.driver = driver;
    }
   By allcustomersField = By.tagName("");
   By addressField = By.tagName("");
   By messageField = By.tagName("");
    By sendButton = By.tagName("");

    public void enterallcustomers(String allcustomers) {driver.findElement(allcustomersField).sendKeys("");
    }
    public void enteraddress(String address) {driver.findElement(addressField).sendKeys("");
    }
    public void entermessage(String message) {driver.findElement(messageField).sendKeys("");
    }
    public void clicksend() {driver.findElement(sendButton).click();
    }
}
*/